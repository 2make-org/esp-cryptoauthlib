# For ESP-IDF 6.x+ (mbedtls 4.x):
# - Use the direct CryptoAuthLib APIs (atcab_sign, atcab_verify_extern, etc.)
# - The mbedTLS ALT integration is NOT compatible with mbedtls 4.x

# For ESP-IDF 5.x and earlier (mbedtls 2.x/3.x), you can enable:
# CONFIG_ATCA_MBEDTLS_ECDSA=y    # For mbedTLS ALT integration
